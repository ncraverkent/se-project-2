#pragma once

namespace WEP
{
	/**
	* @enum EventType
	* Is the type of event that can be scheduled
	*/
	enum class EventType
	{
		Wedding, /** Wedding event */
		Birthday, /** Birthday event */
		Funeral, /** Funeral event */
	};
}